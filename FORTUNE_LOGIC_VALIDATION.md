# FORTUNE_LOGIC_VALIDATION.md — 占術計算ロジックと検証状況

最終更新: 2026-08-10

このファイルは「入力 → 計算 → 分類 → 基礎データ取得 → 解釈」の各段が
**決定論的にプログラム側で確定しているか**を占術ごとに記録する。

凡例
- **検証済**: 既知の正解値・独立した式で突合し一致を確認
- **未検証**: 実装はあるが正解データとの突合をしていない
- **近似**: 実装者自身が近似と明記
- **不正**: 誤り・捏造を確認

---

## 1. 西洋占星術

| 要素 | 実装 | 入力 | 計算 | 検証状況 |
|---|---|---|---|---|
| 太陽星座 | `calcSunSign` | 月日 | 境界日テーブル | **検証済**（境界8日全一致：3/20魚→3/21牡羊、12/21射手→12/22山羊、1/19山羊→1/20水瓶、2/18水瓶→2/19魚） |
| 月星座 | `Ephemeris.allSigns().moon` | 年月日時 | Schlyter簡略式 | **検証済（挙動）**：時刻で 10↔11 と変化。絶対精度は未突合 |
| 水星・金星・火星・木星・土星 | 同上 | 年月日時 | Schlyter簡略式 | 未検証（±0.5°を称する） |
| アセンダント（時刻あり） | `calcAscendant` | 年月日時＋緯度経度 | GMST→LST→ASC式 | **検証済（挙動）**：0/6/12/18/23時で 2→5→9→11→1、沖縄のみ他県と異なる |
| アセンダント（時刻なし） | 同上 | 年月日 | `sun + (d % 12)` | **不正**：天文的根拠なし。擬似値を本物として提示 |
| ハウス | `HOUSES` | ASC | 等ハウス制 | 実装として簡易（プラシダス非対応、本文で自認済み） |
| アスペクト | `calcAspects` | 7天体の星座 | 星座差 0/2/3/4/6 | 簡易：**オーブ（度数）を使わず星座差のみ**。実際の角度と乖離しうる |
| トランジット | `calcTransitHouse` | 本日日付＋ASC | 太陽・月の通過ハウス | 実装通り |

**フォールバック時の挙動**: `window.Ephemeris` が無い場合、各惑星は `daysBetween/一定日数` の粗い近似に落ちる。現状 ephemeris.js は常に読み込まれるため通常は発生しない。

---

## 2. 数秘術

| 要素 | 実装 | 計算 | 検証状況 |
|---|---|---|---|
| ライフパス | `calcLifePath` | 年月日の各桁和→数字根（11/22/33保持） | **検証済**（ピタゴラス式として正しい） |
| 誕生数 | `calcBirthNumber` | 日→数字根 | 検証済 |
| 魂数 | `calcSoulNum` | 地格→数字根 | **姓名画数に依存**（§4の欠落の影響を受ける） |
| 人格数 | `calcPersonalityNum` | 人格→数字根 | 同上 |
| 成熟数 | `calcMaturityNum` | LP+魂数→数字根 | 同上 |

---

## 3. 四柱推命

| 要素 | 実装 | 計算 | 検証状況 |
|---|---|---|---|
| 日柱干支 | `calcDay60` | 2000-01-01=戊午(54)からの日数差 mod 60 | **検証済**：JD法 `(JDN+49) mod 60` と一致。2024-01-01=甲子、2026-08-08=甲寅も整合 |
| 年柱 | `calcYearStem/Branch` | (年−4) mod、立春(2/4固定)補正 | **検証済（挙動）**：2000/2/3=己卯、2/5=庚辰。※立春は毎年2/3〜2/5と変動するが**2/4固定**＝境界日に最大1日の誤り |
| 月支 | `calcMonthBranch` | 太陽黄経（Ephemeris）で節入り判定 | 実装は正攻法。絶対値は未突合 |
| 月干 | `calcMonthStem` | 五虎遁 `(yearStem%5)*2 + monthBranch` | 式として妥当。未突合 |
| 時柱 | `calcHourBranch/Stem` | 五鼠遁、子時23-1時 | 式として妥当。未突合 |
| 通変星 | `calcTsuuhen` | 五行関係×陰陽 | ロジック健全 |
| 蔵干 | `calcZouKan` | 各支の主蔵干のみ | 簡易：**本気/中気/余気を区別しない** |
| 大運 | `calcDaiUn` | 月柱起算・順逆 | **不正**：`app.js:543` で性別が **1 固定**。男性は逆向きの運が出る。起運年齢も**5歳固定**（本来は節入りまでの日数から算出） |
| 用神・忌神 | `calcYoujin` | 五行カウントの最小/最大 | 簡易：旺衰・調候を見ていない |

---

## 4. 姓名判断

| 要素 | 実装 | 検証状況 |
|---|---|---|
| 画数取得 | `strokesOf` ＋ `SeimeiStrokes` 辞書 | **不正リスク大**：辞書未収録の漢字を**一律10画**として計算 |
| 未収録の実測 | 頻出56字中 **理・奈・那・乃・斎** が未収録（他に「々」も） | 「真理子」「奈々」「乃愛」「斎藤」等で五格が誤る |
| 五格 | 天格/人格/地格/外格/総格、1字姓名に霊数1 | 熊崎式として構造は妥当 |
| 旧字体 | 未対応 | 姓名判断は本来**旧字体画数**を用いる流派が主流。新字体基準である旨の明示なし |

**サイレント失敗**：誤った画数でも警告が出ず、ユーザーも運営も気づけない。

---

## 5. 算命学

| 要素 | 実装 | 検証状況 |
|---|---|---|
| 主星（SANMEI） | 日干をそのままインデックス化 | 簡易（本来は日干×日支の陰陽五行から算出） |
| 12従星 | `calc12Jusho` 天将星位置からの距離、陽干順行/陰干逆行 | 構造は妥当。**未検証** |
| 宿命星（月支・日支） | `calcShukumeisei` 蔵干×通変 | 構造は妥当。**未検証** |
| 干合・支合・冲・刑 | `calcKanshiRelations` | 干合/支合/冲は正しいペア表。**刑は簡易セット** |

---

## 6. 九星気学

| 要素 | 実装 | 検証状況 |
|---|---|---|
| 本命星 | `calcNineStar` 11−数字根、立春補正 | **検証済**：2024=三碧、1980=二黒（9年周期逆算と一致） |
| 月命星 | `calcMonthlyStar` 年支3グループ×月支 | 伝統表準拠。未突合 |
| 日命星 | `calcDailyStar` | **近似（コードが自認）**：甲子日暦がなく通日から算出。陽遁/陰遁の境界も簡易 |

---

## 7. 六星占術

| 要素 | 実装 | 検証状況 |
|---|---|---|
| 運命数 | `(西暦下2桁 + 通日 + 日) mod 60`、閏年補正 | **未検証（要確認）**：公式は年ごとの「年数表」を用いる。本実装が一致する保証がない |
| 星の割当 | 運命数10区切りで6星 | 運命数が正しければ妥当 |
| 陽陰 | 運命数の奇偶 | 同上 |

→ **最優先で正解データとの突合が必要。突合できなければ停止を推奨。**

---

## 8. 動物占い（個性心理學60）

| 要素 | 実装 | 検証状況 |
|---|---|---|
| 運命数・60キャラ | `animal60.js` の内蔵表 | 表ベースのため内部整合は取れている。公式表との突合は未実施 |

---

## 9. タロット

| 要素 | 実装 | 検証状況 |
|---|---|---|
| 7ポジション・スプレッド | `drawTarotSpread(y,m,d)` 決定論シード＋重複回避 | **関数は正常。呼び出しが不正**（`app.js:1757` で `p.year/p.month/p.day`＝undefined を渡している）→ **全ユーザー同一** |
| ケルト十字10枚 | `drawCelticCross(y,m,d)` 決定論Fisher-Yates、78枚 | **正常**（生年月日を正しく使用） |
| 小アルカナ1枚 | `calcMinorTarot` | 正常 |
| 運命カード | `fateTarot` LP→大アルカナ対応 | 正常 |
| 正逆位置 | シードのビットから決定 | 正常（ただし7ポジションは上記バグの影響下） |
| 乱数版 `drawTarot` | 未使用（死蔵） | FAQ「引き直すたびに変わる」と矛盾 |

---

## 10. 手相・人相（写真）

| 要素 | 実装 | 検証状況 |
|---|---|---|
| 画像特徴抽出 | 64×64にリサイズし **平均RGB／輝度／標準偏差／左右対称性／彩度／中心-外周比／アスペクト比** | 実装通り（外部送信なし＝◎） |
| タイプ判定 | 上記特徴＋生年月日のFNVハッシュ % DB件数 | **重要**：ハッシュは特徴を"混ぜる"だけで、**特徴とタイプの間に意味的対応がない**（例：線が濃い→実行力型、という写像になっていない） |
| 観察コメント | 輝度・コントラスト・彩度・対称性の閾値分岐 | **特徴と文言の対応はある**。ただし「掌線がはっきり濃い」の判定は画像全体の標準偏差であり、**掌線そのものは検出していない** |
| 画質チェック | **なし** | ぼけ・暗所・手以外の写真でも診断が出る。再撮影案内なし |
| 健康・運気への言及 | あり（「健康運・実行運が好調」等） | §26 の禁止事項に接近。要文言調整 |

---

## 11. その他

| 占術 | 実装 | 検証状況 |
|---|---|---|
| 易経（64卦） | `calcIching` 決定論ハッシュ | 卦の選定は本来「筮竹/コイン」の作法。ハッシュである旨の説明なし |
| 之卦・変爻 | `calcHengaIching` `main+(line+1)*7 mod 64` | **不正に近い**：変爻から之卦を求める伝統的手順（該当爻の陰陽反転）ではない |
| ルーン | `calcRune` 決定論ハッシュ | 正逆なし |
| 夢占い | `calcDream` 生年月日ハッシュ | **夢の内容を入力できない**＝夢占いとして機能していない |
| オラクル / おみくじ | 未使用 | 死蔵 |

---

## 12. 次に作るべき検証テスト（未実装）

- [ ] 日柱干支：既知10日付との突合（自動テスト）
- [ ] 六星占術：公式年数表との突合（**最優先**）
- [ ] 姓名判断：常用漢字＋人名用漢字の辞書カバー率測定と欠落リスト出力
- [ ] 節入り境界：2/3〜2/5生まれの年干支切替（各年の実際の立春時刻と比較）
- [ ] 時刻境界：23:00〜01:00（子時）で日柱が変わるか（**現状：日柱は暦日で判定＝夜子時未対応**）
- [ ] うるう年：2/29生まれの全占術
- [ ] 入力欠損：時刻なし・氏名なし・都道府県なしで全占術が破綻しないか
